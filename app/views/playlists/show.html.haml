%h1.page-header
  = @playlist.name
%ul
  - @playlist.tracks.each do |track |
    %li
      = track.name
      |
      %span{ :class => "glyphicon glyphicon-star-empty"}
      //glyphicon glyphicon-star